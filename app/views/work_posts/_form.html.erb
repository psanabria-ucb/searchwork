<%= form_for(@work_post) do |f| %>
  <% if @work_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_post.errors.count, "error") %> prohibited this work_post from being saved:</h2>

      <ul>
      <% @work_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
